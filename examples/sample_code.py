"""Tiny sample for examples/code_review.yaml."""


def add(left: int, right: int) -> int:
    return left + right


def divide(left: float, right: float) -> float:
    return left / right


def first(items: list[str]) -> str:
    return items[0]
